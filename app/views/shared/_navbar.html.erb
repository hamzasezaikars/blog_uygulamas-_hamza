<nav class="navbar navbar-expand-lg bg-body-tertiary bg-light">
  <div class="container-fluid">
    <%= link_to "Blogger", root_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Yazılarım", user_posts_path(current_user), class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Hakkımda", user_profile_path(current_user), class: "nav-link" %>
          </li>

          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
              data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= current_user.fullname %>
            </a>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to "Yeni Post Oluştur", new_user_post_path(current_user), class: "dropdown-item" %>
              <%= link_to "Yeni Kategori Oluştur", new_category_path, class: "dropdown-item" %>
              <div class="dropdown-divider"></div>
              <%= link_to "<i class='bi bi-box-arrow-right'></i> Çıkış Yap".html_safe,
                          destroy_user_session_path,
                          method: :delete,
                          data: { turbo_method: :delete },
                          class: "dropdown-item text-danger" %>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Kayıt Ol", new_user_registration_path, class: "btn btn-success btn-sm rounded me-3" %>
          </li>
          <li class="nav-item">
            <%= link_to "Giriş Yap", new_user_session_path, class: "btn btn-outline-primary btn-sm" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
